/*
 * The following are domain model string constants
 * that are used to determine how to interpret the
 * data in the workflow node.
 */
// export const DRAW_CONFIG_DOMAIN_MODEL = "DrawConfig";
export const DRAW_APPLICANT_DOMAIN_MODEL = "APPLICANTS";
// Draw is repeated twice in the name, but it's not a typo.
// The first Draw is the domain, the second is the model.
export const DRAW_DRAW_DATA_DOMAIN_MODEL = "HUNT_CODES";
export const DRAW_METRICS_DOMAIN_MODEL = "DRAW_METRICS";

// TODO: this should be blown away...
export const DRAW_RESULT_EXPORT_DOMAIN_MODEL = "DrawResultExport";
